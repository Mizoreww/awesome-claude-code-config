---
description: "TypeScript/JavaScript hooks: Prettier formatting, tsc type-checking, and console.log warnings/audits."
globs: "**/*.{ts,tsx,js,jsx}"
alwaysApply: false
---

# TypeScript/JavaScript Hooks

> Extends the common hooks rule with TypeScript/JavaScript-specific content.

## afterFileEdit Hooks

Configure in `~/.cursor/hooks.json`:

- **Prettier**: Auto-format JS/TS files after edit
- **TypeScript check**: Run `tsc` after editing `.ts`/`.tsx` files
- **console.log warning**: Warn about `console.log` in edited files

## stop Hooks

- **console.log audit**: Check all modified files for `console.log` before session ends
