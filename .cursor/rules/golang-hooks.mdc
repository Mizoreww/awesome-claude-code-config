---
description: "Go hooks: auto-format .go files with gofmt/goimports and run go vet plus staticcheck on edit."
globs: "**/*.go"
alwaysApply: false
---

# Go Hooks

> Extends the common hooks rule with Go-specific content.

## afterFileEdit Hooks

Configure in `~/.cursor/hooks.json`:

- **gofmt/goimports**: Auto-format `.go` files after edit
- **go vet**: Run static analysis after editing `.go` files
- **staticcheck**: Run extended static checks on modified packages
