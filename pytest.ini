[pytest]
testpaths = tests
markers =
    unit: fast behavior tests without external processes
    integration: public CLI and filesystem contract tests
    e2e: browser-level interaction tests (strict gate sets PAPER_READING_REQUIRE_BROWSER=1)
